// Find the longest string till Ctrl-D is pressed
// If there are multiple strings of the same length, the earlier string that was
// inputted will be printed out

#include <stdio.h>
#include <string.h>

#define MAX_NAME_LEN 200

int main (void) {

    return 0;
}
